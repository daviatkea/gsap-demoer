---
// import { Image } from "astro:assets";
import { getImage } from "astro:assets";
import heroImg from "../assets/imgs/hero.jpg";
import { Icon } from "astro-icon/components";

const optimizedHero = await getImage({ src: heroImg, format: "webp" });
---

<div class="hero" style={`background-image: url(${optimizedHero.src});`}>
  <!-- <Image src={heroImg} format="webp" alt="Hero" /> -->
  <div class="content">
    <h1>Lorem ipsum dolor sit amet optio quos elit.</h1>
    <p>
      Lorem ipsum dolor sit amet consectetur adipisicing elit. Eos quos itaque
      recusandae fugit optio nesciunt vitae.
    </p>
    <Icon name="solar:danger-triangle-line-duotone" />
    <small>Tekst: Lorem ipsum dolor sit.</small>
  </div>
</div>

<p class="manchet">
  Lorem ipsum dolor sit amet consectetur adipisicing elit. Commodi asperiores
  voluptatum at odit laboriosam facilis fuga porro excepturi molestiae tenetur!
</p>

<style>
  .manchet {
    font-size: var(--size-fluid-2);
    grid-column: 2;
  }

  [data-icon] {
    color: pink;
    inline-size: 3rem;
    block-size: 3rem;
  }

  .hero {
    background: #fe095d;
    color: #fff;
    display: inherit;
    grid-template-columns: inherit;
    grid-column: 1 / -1;
    align-content: start;
    min-height: 70dvh;
    place-content: center;

    background-position: 0% 50%;
    background-size: cover;
    background-blend-mode: darken;

    .content {
      display: grid;
      gap: 1rem;
      grid-column: 2;

      > h1 {
        text-align: center;
        font-size: var(--size-fluid-5);
      }
    }
  }
</style>
